<Page
    x:Class="FotogramClient.CadastroUsuario"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FotogramClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontSize="50">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel Margin="20">
                <TextBlock Text="Nome de completo"/>
                <TextBox x:Name="TxTNomeCompleto" />
                <TextBlock Text="Nome de usuário"/>
                <TextBox x:Name="TxTNomeUsuario" />
                <TextBlock Text="Senha"/>
                <PasswordBox x:Name="TxtSenha" />
                <TextBlock Text="Confirmação"/>
                <PasswordBox x:Name="TxtConfirmacaoSenha" />
                <TextBlock Text="Data nascimento"/>
                <DatePicker x:Name="DtpDataNascimento"/>
                <TextBlock Text="Email"/>
                <TextBox x:Name="TxtEmail"/>
                <Button x:Name="BtnCadastro" Content="Cadastrar" 
                    Click="BtnCadastro_Click" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
